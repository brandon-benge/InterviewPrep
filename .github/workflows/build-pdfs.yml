name: Build PDFs

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container: pandoc/latex:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install bash and dependencies
        run: |
          apk add --no-cache bash findutils

      - name: List files (debug)
        run: ls -la

      - name: Make script executable
        run: chmod +x convert_md_to_pdf.sh

      - name: Generate PDFs
        run: bash convert_md_to_pdf.sh

      - name: Upload PDFs artifact
        uses: actions/upload-artifact@v4
        with:
          name: interview-prep-pdfs
          path: "**/*.pdf"
          if-no-files-found: warn
          retention-days: 30